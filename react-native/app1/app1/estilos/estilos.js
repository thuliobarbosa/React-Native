import {
  StyleSheet
} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center'
  },

  textoCurso: {
    color: '#00f',
    fontSize: 15
  },

  textoPadrao: {
    color: '#000',
    fontSize: 20
  },

  textTitulo: {
    color: '#f00',
    fontSize: 30
  }
});